# Definition of Done

Completion requires all items below:

- `git status` output is captured.
- `git diff --stat` output is captured.
- Build/test verification logs are captured when applicable.
- Smoke check outputs are captured.
- Commit hash is recorded.
- Rollback commands are provided and validated for feasibility.

No task is complete without proof artifacts for every applicable item.
