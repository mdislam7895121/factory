import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Activation Analytics', robots: { index: false, follow: false } };
export default function AdminOnboardingLayout({ children }: { children: React.ReactNode }) { return children; }
