# Production Environment Variables for Web
# Copy this file to .env.production and update with production values

# API Endpoint (should point to your production API)
NEXT_PUBLIC_API_URL=https://api.factory.com

# Node Environment (always production in this context)
NODE_ENV=production

# Optional: Add other production-specific environment variables here
# Environment variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# NEXT_PUBLIC_APP_NAME
# NEXT_PUBLIC_ANALYTICS_ID
# NEXT_PUBLIC_SENTRY_DSN
# etc.
#
# Non-public environment variables (without NEXT_PUBLIC_) are server-only
# SOME_SECRET_KEY
# etc.

# Optional: Browser error monitoring (safe if empty)
NEXT_PUBLIC_SENTRY_DSN=
